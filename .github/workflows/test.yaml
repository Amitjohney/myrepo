name: pull_request_flow
on: 
  pull_request:
    types:
    - opened
    branches:
    - testbranch
jobs:
  testscript:
    runs-on: ubuntu-latest
    steps:
      - name: checkoutut
        run: echo "Hello world"
      